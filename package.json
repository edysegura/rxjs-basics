{
  "private": true,
  "scripts": {
    "distinct": "parcel ./distinct-until-changed/index.html -d dev --open",
    "filter": "parcel ./filter/index.html -d dev --open",
    "from-array": "parcel ./from-array/index.html -d dev --open",
    "from-event": "parcel ./from-event/index.html -d dev --open",
    "interval": "parcel ./interval/index.html -d dev --open",
    "merge-map": "parcel ./merge-map/index.html -d dev --open",
    "switch-map": "parcel ./switch-map/index.html -d dev --open",
    "take": "parcel ./take/index.html -d dev --open"
  },
  "dependencies": {
    "rxjs": "^6.5.3"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
